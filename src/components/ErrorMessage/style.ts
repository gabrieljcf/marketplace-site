import styled from "styled-components";

export const ErrorContainer = styled.div`
  margin-top: 2rem;
    text-align: center;

    span {
      color: var(--pink);
      font-weight: 600;
      font-size: 1.2rem;
    }
`;