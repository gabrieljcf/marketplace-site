import styled from "styled-components";

export const Container = styled.section`
  max-width: 1000px;
  margin: 0 auto;

  h2 {
    margin-top: 4rem;
    text-align: center;
    font-size: 2rem;
    color: var(--text-body);

    span {
      color: var(--pink-dark)
    }
  }
`;