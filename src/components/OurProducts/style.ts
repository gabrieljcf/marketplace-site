import styled from "styled-components";

export const Container = styled.section`
  max-width: 1120px;
  margin: 4rem auto;

  h2 {
    text-align: center;
    font-size: 2rem;
    color: var(--text-body);

    span {
      color: var(--pink-dark)
    }
  }
`;